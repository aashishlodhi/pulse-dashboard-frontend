# URL of the Pulse Dashboard sync backend (see /server).
# Leave this blank/unset to run the dashboard in local-only mode (no MongoDB, no cross-device sync).
# Example: https://pulse-dashboard-api.onrender.com
VITE_API_URL=
